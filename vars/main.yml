# ========= Cluster configuration ====== #

# if need to switch smth in broker binary
custom_broker_bin: False

# if need to switch smth in gateway binary
custom_gateway_bin: False


# Arch(used in conf file)
cluster_configuration:
  broker:
    - 1.1.1.3
    - 1.1.1.4
    - 1.1.1.5
  gateway:
    - 1.1.1.1
    - 1.1.1.2